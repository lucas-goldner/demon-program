import React from 'react';
import styled from 'styled-components';

const Titel = styled.h1`
    font-size: 5em;
    text-decoration: underline;
`

const Title = () =>{
    return(
        <Titel>Demon Program</Titel>
    )
}

export default Title;